import type { PluginManifest } from '../shared/types';
import { youtubeShortsMeta } from './youtube-shorts-disabler/manifest';

// Add new plugin manifests here. No other files need to change.
export const pluginManifests: PluginManifest[] = [youtubeShortsMeta];
